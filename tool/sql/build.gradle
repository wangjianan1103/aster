buildscript {
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath 'org.flywaydb:flyway-gradle-plugin:3.2.1'
    }
}

apply plugin: 'org.flywaydb.flyway'
apply plugin: 'java'

flyway {
    url = 'jdbc:mysql://localhost:3306/aster?useUnicode=true&characterEncoding=utf8'
    user = 'wangjianan'
    password = 'Ai1103@.com'
    schemas = ['aster']
    locations = ["filesystem:migrations/"]
}

repositories {
    mavenCentral()
}

dependencies {
    compile 'mysql:mysql-connector-java:5.1.35'
}
